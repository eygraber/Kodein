if(!project.hasProperty('BINTRAY_USER')) project.ext.BINTRAY_USER = 'eygraber'
if(!project.hasProperty('ARTIFACT_GROUP')) project.ext.ARTIFACT_GROUP = 'com.staticbloc.kodein'
if(!project.hasProperty('BINTRAY_REPO')) project.ext.BINTRAY_REPO = 'kodein'
if(!project.hasProperty('BINTRAY_ORG')) project.ext.BINTRAY_ORG = 'staticbloc'
if(!project.hasProperty('USE_DOKKA')) project.ext.USE_DOKKA = true
if(!project.hasProperty('LICENSE')) project.ext.LICENSE = 'MIT'
if(!project.hasProperty('LICENSE_URL')) project.ext.LICENSE_URL = 'https://opensource.org/licenses/MIT'
if(!project.hasProperty('VCS_URL')) project.ext.VCS_URL = 'https://github.com/eygraber/Kodein'
if(!project.hasProperty('ARTIFACT_VERSION')) project.ext.ARTIFACT_VERSION = '3.2.1'